Inicialmente utilizar o método requests.get para acessar a página web.

A biblioteca beautifulsoup4 permite navegar pelas classes e IDs da URL 
coletada com o método requests.

Ainda utilizando o beautifulsoup4, nós podemos filtrar os links existentes nessa
página utilizando o find_all('a'), que é a tap "a hred" do HTML.

Uma vez coletado todas as URLs utilizaremos uma forma de vetorizar as mesmas.